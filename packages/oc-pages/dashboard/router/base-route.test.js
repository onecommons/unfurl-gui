import {baseRoute} from './base-route';

/*
test('basic dashboard route', () => {
    expect(baseRoute('/user/dashboard')).toEqual('/user/dashboard/');
});

test('basic dashboard route arriving at environment', () => {
    expect(baseRoute('/user/dashboard/environment')).toEqual('/user/dashboard/');
});

test('basic dashboard route arriving at deployments', () => {
    expect(baseRoute('/user/dashboard/deployments')).toEqual('/user/dashboard/');
});

test('canonical dashboard route', () => {
    expect(baseRoute('/user/dashboard/-/unfurl/home')).toEqual('/user/dashboard/-/unfurl/');
});
*/
