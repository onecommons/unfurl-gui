import {default as FileSelectorComponent} from './file-selector.vue'

export const FileSelector = async function() {
    // if we want to async import anything for this component, we should do it here

    return FileSelectorComponent
}

export default FileSelector
