mutation createTemplate(
  $projectPath: ID!
  $template: TemplateInput!
) {
  createTemplate(
    input: {
      projectPath: $projectPath
      template: $template
    }
  ) {
    templates {
        title
    }
    errors
  }
}
