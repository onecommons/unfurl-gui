mutation updateOverview(
  $projectPath: ID!
  $title: String
  $template: TemplateInput
  $inputs:  [InputInput!]
) {
  updateOverview(
    input: {
      projectPath: $projectPath
      title: $title
      template: $template
      inputs: $inputs
    }
  ) {
    isOk
    errors
  }
}