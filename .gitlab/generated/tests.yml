sanity:
  extends: .tests
  script:
    - " yarn run integration-test run --username nobody --namespace
      $OC_NAMESPACE -- --browser chrome -s './cypress/e2e/00_visitor/*.js'"
  rules:
    - if: $TEST == "sanity" || $TEST == "visitor" || $TEST == "all"
nestedcloud:
  extends: .tests
  script:
    - " yarn run integration-test run  --namespace $OC_NAMESPACE -- --browser
      chrome -s './cypress/e2e/blueprints/*nestedcloud*.js'"
  rules:
    - if: $TEST == "nestedcloud" || $TEST == "all"
nextcloud_aws:
  extends: .tests
  script:
    - " yarn run integration-test run  --namespace $OC_NAMESPACE -- --browser
      chrome -s './cypress/e2e/blueprints/aws*nextcloud*.js'"
  rules:
    - if: $TEST == "nextcloud" || $TEST == "nextcloud_aws" || $TEST == "all"
nextcloud_gcp:
  extends: .tests
  script:
    - " yarn run integration-test run  --namespace $OC_NAMESPACE -- --browser
      chrome -s './cypress/e2e/blueprints/gcp*nextcloud*.js'"
  rules:
    - if: $TEST == "nextcloud" || $TEST == "nextcloud_gcp" || $TEST == "all"
nextcloud_do:
  extends: .tests
  script:
    - " yarn run integration-test run  --namespace $OC_NAMESPACE -- --browser
      chrome -s './cypress/e2e/blueprints/do*nextcloud*.js'"
  rules:
    - if: $TEST == "nextcloud" || $TEST == "nextcloud_do" || $TEST == "all"
baserow:
  extends: .tests
  script:
    - " yarn run integration-test run  --namespace $OC_NAMESPACE -- --browser
      chrome -s './cypress/e2e/blueprints/*baserow*.js'"
  rules:
    - if: $TEST =~ /baserow/ || $TEST == "all"
minecraft:
  extends: .tests
  script:
    - " yarn run integration-test run  --namespace $OC_NAMESPACE -- --browser
      chrome -s './cypress/e2e/blueprints/*minecraft*.js'"
  rules:
    - if: $TEST =~ /minecraft/ || $TEST == "all"
container_webapp:
  extends: .tests
  script:
    - EXTERNAL=0 yarn run integration-test run  --namespace $OC_NAMESPACE --
      --browser chrome -s './cypress/e2e/blueprints/*container-webapp*.js'
  rules:
    - if: $TEST == "container_webapp" || $TEST == "all"
container_webapp_gcp:
  extends: .tests
  script:
    - EXTERNAL=0 yarn run integration-test run  --namespace $OC_NAMESPACE --
      --browser chrome -s './cypress/e2e/blueprints/gcp__container-webapp*.js'
  rules:
    - if: $TEST == "container_webapp_gcp"
container_webapp_aws:
  extends: .tests
  script:
    - EXTERNAL=0 yarn run integration-test run  --namespace $OC_NAMESPACE --
      --browser chrome -s './cypress/e2e/blueprints/aws__container-webapp*.js'
  rules:
    - if: $TEST == "container_webapp_aws"
ghost:
  extends: .tests
  script:
    - " yarn run integration-test run  --namespace $OC_NAMESPACE -- --browser
      chrome -s './cypress/e2e/blueprints/*ghost*.js'"
  rules:
    - if: $TEST =~ /ghost/ || $TEST == "all"
mediawiki:
  extends: .tests
  script:
    - " yarn run integration-test run  --namespace $OC_NAMESPACE -- --browser
      chrome -s './cypress/e2e/blueprints/*mediawiki*.js'"
  rules:
    - if: $TEST =~ /mediawiki/ || $TEST == "all"
wordpress:
  extends: .tests
  script:
    - " yarn run integration-test run  --namespace $OC_NAMESPACE -- --browser
      chrome -s './cypress/e2e/blueprints/*wordpress*.js'"
  rules:
    - if: $TEST =~ /wordpress/ || $TEST == "all"
kubernetes:
  extends: .tests
  script:
    - " yarn run integration-test run  --namespace $OC_NAMESPACE -- --browser
      chrome -s './cypress/e2e/blueprints/k8s*.js'"
  rules:
    - if: $TEST =~ /k8s/ || ($TEST == "all" && $SKIP !~ /k8s/)
unfurl_cloud_dns:
  extends: .tests
  script:
    - USE_UNFURL_DNS=opencloudservices.net yarn run integration-test
      run  --namespace $OC_NAMESPACE -- --browser chrome -s
      './cypress/e2e/blueprints/aws__nextcloud__only-mail*.js'
  rules:
    - if: $TEST =~ /uc_dns/ || $TEST == "all"
cloud_redis:
  extends: .tests
  script:
    - " yarn run integration-test run  --namespace $OC_NAMESPACE -- --browser
      chrome -s
      'cypress/e2e/blueprints/gcp__nextcloud__memorystore*.js,cypress/e2e/bluep\
      rints/aws__nextcloud__memorydb*.js'"
  rules:
    - if: $TEST == "cloud_redis"
multiple_workflows:
  extends: .tests
  script:
    - " yarn run integration-test run  --namespace $OC_NAMESPACE -- --browser
      chrome -s 'cypress/e2e/deployments/multiple-workflows*.js'"
  rules:
    - if: $TEST == "multiple_workflows"
nextcloud_shared_volumes:
  extends: .tests
  script:
    - " yarn run integration-test run  --namespace $OC_NAMESPACE -- --browser
      chrome -s 'cypress/e2e/deployments/shared-volume*.js'"
  rules:
    - if: $TEST == "shared_volumes" || $TEST == "all"

